@model Employee
@{
    ViewData["Title"] = "Create";

    SelectList selectListItems = new SelectList(ViewBag.depts, "Id", "Name");
}

<!-- name age salary address deptId-->
<h1>Create</h1>
<!--
<form method="post" action="/Employee/Create">
    <div>
        <label for="Name">Name</label>
        <input type="text" name="Name" id="Name" value="@Model.Name" class="form-control"/>
       @*  @Html.TextBox("Name",value:Model.Name,htmlAttributes:new {@class="form-control"}) *@
      @*  @Html.TextBoxFor((x)=>x.,"Name") *@
     
    </div>

    <div>
        <label for="Age">Age</label>
        <input type="number" name="Age" id="Age" value="@Model.Age" class="form-control" />
    </div>
    <div>
        <label for="Salary">Salary</label>
        <input type="number" name="Salary" id="Salary"  value="@Model.Salary"class="form-control" />
    </div>

    <div>
        <label for="Address">Address</label>
        <input type="text" name="Address" id="Address" value="@Model.Address" class="form-control" />
    </div>

    <div>
        <label for="DeptId">DeptId</label>
        <select name="DeptId" class="form-control">
         @*    @foreach (var item in ViewBag.depts)
            {
                <option value="@item.Id">@item.Name</option>
            } *@
        </select>
    </div>

    <div>
        <input type="submit" class="btn btn-primary" value="Create New " />
    </div>
</form>
-->
<form method="post" asp-action="Create" asp-controller="Employee">
    <div>
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
    </div>

    <div>
        <label for="Age">Age</label>
        <input asp-for="Age" class="form-control" />
    </div>
    <div>
        <label asp-for="Salary"></label>
        <input asp-for="Salary" class="form-control" />
    </div>

    <div>
        <label asp-for="Address"></label>
        <input asp-for="Address" class="form-control" />
    </div>

    <div>
        <label asp-for="DeptId"></label>
        <select class="form-control" asp-for="DeptId" asp-items="selectListItems">
           
        </select>
    </div>

    <div>
        <input type="submit" class="btn btn-primary" value="Create New " />
    </div>
</form>